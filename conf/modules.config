/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]


    withName: 'BBDUK_CUSTOM' {
        publishDir = [
            path: { "${params.outdir}/trimmed" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
        cpus = params.threads
        ext.args = { 'ktrim=r k=23 mink=11 prealloc=t qhdist=1 tpe=t tbo=t minlen=36 -da' }
    }

    withName: 'FASTQC' {
        publishDir = [
            path: { "${params.outdir}/fastqc" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '5 GB'
        cpus = 2
        ext.args = '--quiet'
    }

    withName: 'MINIMAP2_ALIGN' {
        publishDir = [
            path: { "${params.outdir}/aligned_l" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
    }

    withName: 'MINIMAP2_INDEX' {
        publishDir = [
            path: { "${params.outdir}/mm2i" },
            mode: 'copy',
            overwrite: true
        ]
    }

    withName: 'BWAMEM3_INDEX' {
        publishDir = [
            path: { "${params.outdir}/bw3i" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
        ext.args = '--max-memory 15G'
    }

    withName: 'BWAMEM3_MEM' {
        publishDir = [
            path: { "${params.outdir}/aligned_s" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
    }

    withName: 'SAMTOOLS_STATS' {
        publishDir = [
            path: { "${params.outdir}/aligned_stats" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
        ext.args = '--max-memory 15G'
    }

    withName: 'SAMTOOLS_FAIDX' {
        publishDir = [
            path: { "${params.outdir}/download" },
            mode: 'copy',
            overwrite: true
        ]
        memory = '15 GB'
    }

    withName: 'MULTIQC' {
        ext.args   = { params.multiqc_title ? "--title \"$params.multiqc_title\"" : '' }
        publishDir = [
            path: { "${params.outdir}/multiqc" },
            mode: params.publish_dir_mode
        ]
    }

    withName: 'GATK4_CLEANSAM' {
        publishDir = [
            path: { "${params.outdir}/clean_bams" },
            mode: 'copy',
            overwrite: true
        ]
        ext.prefix = { "${meta.id}.clean" }
    }

    withName: 'GATK4_MARKDUPLICATES_LOCAL' {
    publishDir = [
        path: { "${params.outdir}/clean_bams" },
        mode: 'copy',
        overwrite: true
    ]
    ext.prefix = { "${meta.id}.sorted.dup" }
    ext.args   = '--CREATE_INDEX false'   // we index the pf BAM ourselves
    memory     = '15 GB'
    }

}
